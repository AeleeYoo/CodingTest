import time
start = time.time()  # 시작 시간 저장

n = int(input())

array = [0] * n

for i in range(n):
  array[i] = int(input())

array.sort()

for i in array:
  print(i, end=' ')

print("time :", time.time() - start)  # 현재시각 - 시작시간 = 실행 시간
